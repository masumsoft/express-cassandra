engines:
  eslint:
    enabled: true
    channel: "eslint-4"
    checks:
      import/no-unresolved:
        enabled: false
  duplication:
    enabled: true
    config:
      languages:
      - javascript
ratings:
  paths:
    - src/**
exclude_paths:
  - lib/
