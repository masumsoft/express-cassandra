engines:
  eslint:
    enabled: true
    channel: "eslint-3"
    checks:
      import/no-unresolved:
        enabled: false
  duplication:
    enabled: true
    config:
      languages:
      - javascript
ratings:
  paths:
    - src/**
exclude_paths:
  - lib/
